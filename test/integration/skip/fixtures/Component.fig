template
	.__fig-skip(id='potato')
		.x hi
			.y= foo

	.ne
		.sted
			.__fig-skip(id='nested')
				.snek= bar

script
	export default (view, opts, bus) => {
		view.foo = opts.foo
		view.bar = opts.bar
	}
